---
 - hosts: all
   tasks:
     - name: copy the file from local to remote
       copy:
         src: /var/lib/jenkins/workspace/sonar and ansible/target/my-jenkins-project.war
         dest: /tmp/apache-tomcat-9.0.107/webapps
